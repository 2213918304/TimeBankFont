/* 个人中心页面 - TDesign风格 */

.page-container {
  width: 100%;
  max-width: 100vw;
  height: 100vh;
  background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
}

/* 内容区域 */
.content {
  flex: 1;
  width: 100%;
  max-width: 100vw;
  padding: 24rpx;
  box-sizing: border-box;
}

.content.family-ui {
  padding-bottom: 140rpx;
}

.content.elderly-ui {
  padding-bottom: 160rpx;
}

/* 个人信息卡片 */
.profile-card {
  border-radius: 20rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
  background: white;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
  border: 1rpx solid #f0f4f8;
  transition: all 0.3s ease;
}

.profile-card.family {
  background: white;
}

.profile-card.elderly {
  background: white;
}

.profile-info {
  display: flex;
  align-items: center;
  gap: 24rpx;
  margin-bottom: 24rpx;
  padding-bottom: 20rpx;
  border-bottom: 2rpx solid #f0f0f0;
}

.avatar {
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 40rpx;
  font-weight: 700;
  color: white;
}

.avatar.family {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  box-shadow: 0 4rpx 20rpx rgba(102, 126, 234, 0.3);
}

.avatar.elderly {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  box-shadow: 0 4rpx 20rpx rgba(102, 126, 234, 0.3);
}

.user-info {
  flex: 1;
}

.username {
  font-size: 32rpx;
  font-weight: 700;
  margin-bottom: 12rpx;
  color: #333;
}

.user-tags {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 12rpx;
}

.user-tag {
  padding: 6rpx 12rpx;
  border-radius: 12rpx;
  font-size: 22rpx;
  color: #667eea;
  background: #f0f9ff;
  border: 1rpx solid #bae6fd;
}

.user-desc {
  font-size: 24rpx;
  color: #5d7c9a;
}

.user-meta {
  font-size: 22rpx;
  color: #8b9dc3;
}

.bind-code {
  font-weight: 600;
  color: #f97316;
}

.edit-btn {
  width: 100%;
  padding: 16rpx;
  background: #f8fafc;
  border: 1rpx solid #bae6fd;
  color: #667eea;
  border-radius: 12rpx;
  font-size: 26rpx;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s ease;
}

.edit-btn:active {
  background: #e0f2fe;
  transform: translateY(-1rpx);
}

/* 统计卡片 */
.stats-card {
  background: white;
  border-radius: 20rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
  border: 1rpx solid #f0f4f8;
  box-sizing: border-box;
}

.card-title {
  font-size: 28rpx;
  font-weight: 600;
  margin-bottom: 20rpx;
  color: #333;
  display: flex;
  align-items: center;
  gap: 8rpx;
}

.stats-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16rpx;
}

.stat-item {
  background: #f8fafc;
  border-radius: 12rpx;
  padding: 20rpx;
  text-align: center;
  border: 1rpx solid #e2e8f0;
  box-sizing: border-box;
}

.stat-value {
  font-size: 36rpx;
  font-weight: 700;
  color: #667eea;
  margin-bottom: 8rpx;
}

.stat-label {
  font-size: 22rpx;
  color: #8b9dc3;
}

/* 关联卡片 */
.relation-card {
  background: white;
  border-radius: 20rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
  border: 1rpx solid #f0f4f8;
  box-sizing: border-box;
}

.relation-card.elderly {
  background: white;
}

.relation-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 32rpx;
  gap: 20rpx;
}

.relation-header .card-title {
  flex: 1;
  margin-bottom: 0;
}

.bind-btn {
  background: linear-gradient(135deg, #165DFF, #0040CC);
  color: white;
  border: none;
  border-radius: 20rpx;
  padding: 12rpx 24rpx;
  font-size: 26rpx;
  font-weight: 500;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 8rpx;
  box-shadow: 
    0 4rpx 12rpx rgba(22, 93, 255, 0.3),
    0 2rpx 6rpx rgba(22, 93, 255, 0.2);
  transition: all 0.3s ease;
  min-width: 120rpx;
  justify-content: center;
}

.bind-btn:active {
  background: linear-gradient(135deg, #0040CC, #003399);
  transform: translateY(1rpx);
  box-shadow: 
    0 2rpx 8rpx rgba(22, 93, 255, 0.4),
    0 1rpx 3rpx rgba(22, 93, 255, 0.3);
}

.relation-count {
  font-size: 28rpx;
  color: #6b7280;
}

.elder-item,
.family-item {
  background: #f8f9fa;
  border-radius: 24rpx;
  padding: 32rpx;
  display: flex;
  align-items: center;
  gap: 24rpx;
  box-sizing: border-box;
}

.family-item {
  background: rgba(249, 115, 22, 0.1);
}

.elder-avatar,
.family-avatar {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32rpx;
  font-weight: 600;
  color: white;
}

.elder-avatar {
  background: linear-gradient(135deg, #667eea, #764ba2);
}

.family-avatar {
  background: linear-gradient(135deg, #165DFF, #722ED1);
}

.elder-info,
.family-info {
  flex: 1;
}

.elder-name,
.family-name {
  font-size: 32rpx;
  font-weight: 600;
  margin-bottom: 4rpx;
  color: #1f2937;
}

.elder-contact,
.family-desc {
  font-size: 24rpx;
  color: #6b7280;
}

.manage-btn {
  padding: 12rpx 24rpx;
  font-size: 24rpx;
  border: 2rpx solid #165DFF;
  color: #165DFF;
  background: transparent;
  border-radius: 16rpx;
  cursor: pointer;
}

.contact-btn {
  background: #f97316;
  color: white;
  border: none;
  border-radius: 16rpx;
  padding: 12rpx 24rpx;
  font-size: 24rpx;
  cursor: pointer;
}

.binding-code-section {
  text-align: center;
  margin-top: 32rpx;
}

.binding-code-btn {
  background: transparent;
  color: #f97316;
  border: 4rpx solid #f97316;
  border-radius: 16rpx;
  padding: 16rpx 32rpx;
  font-size: 28rpx;
  cursor: pointer;
}

/* 功能菜单 */
.menu-card {
  border-radius: 24rpx;
  padding: 32rpx 28rpx;
  border: 2rpx solid rgba(22, 93, 255, 0.1);
  box-shadow: 
    0 8rpx 32rpx rgba(22, 93, 255, 0.08),
    0 4rpx 16rpx rgba(114, 46, 209, 0.06),
    0 2rpx 8rpx rgba(0, 0, 0, 0.04);
  box-sizing: border-box;
}

.menu-card.family {
  background: linear-gradient(135deg, #ffffff 0%, #f8faff 50%, #f0f8ff 100%);
}

.menu-card.elderly {
  background: linear-gradient(145deg, #ffffff 0%, #fff8f5 80%, #fff5f0 100%);
  border: 4rpx solid rgba(255, 138, 101, 0.08);
  box-shadow: 
    0 40rpx 120rpx rgba(255, 138, 101, 0.08),
    0 20rpx 64rpx rgba(255, 112, 67, 0.05),
    0 8rpx 32rpx rgba(0, 0, 0, 0.03),
    inset 0 4rpx 0 rgba(255, 255, 255, 0.8);
}

.menu-item {
  padding: 32rpx 0;
  border-bottom: 2rpx solid #f0f0f0;
  display: flex;
  align-items: center;
  justify-content: space-between;
  cursor: pointer;
  transition: all 0.2s ease;
}

.menu-item:last-child {
  border-bottom: none;
}

.menu-item:active {
  background: rgba(22, 93, 255, 0.05);
  margin: 0 -28rpx;
  padding-left: 28rpx;
  padding-right: 28rpx;
  border-radius: 12rpx;
}

.menu-left {
  display: flex;
  align-items: center;
  gap: 24rpx;
}

.menu-icon {
  font-size: 40rpx;
}

.menu-text {
  font-size: 32rpx;
  color: #1f2937;
}

.logout-text {
  color: #F5222D;
}

.menu-arrow {
  color: #999;
  font-size: 28rpx;
}

/* ========== TDesign组件样式 ========== */

/* 头像样式 */
.avatar.family {
  --td-avatar-bg-color: linear-gradient(135deg, #165DFF, #722ED1);
}

.avatar.elderly {
  --td-avatar-bg-color: linear-gradient(135deg, #FF8A65, #FF7043);
}

/* 标签样式 */
.user-tags {
  display: flex;
  gap: 16rpx;
  margin: 12rpx 0;
}

.user-tag {
  margin-right: 8rpx;
}

.verify-tag {
  margin-left: 8rpx;
}

/* 用户信息图标 */
.user-meta {
  display: flex;
  align-items: center;
  gap: 8rpx;
  flex-wrap: wrap;
  margin-top: 8rpx;
}

/* 卡片标题图标 */
.card-title {
  display: flex;
  align-items: center;
  gap: 12rpx;
  font-size: 32rpx;
  font-weight: 600;
  color: #1f2937;
  margin-bottom: 24rpx;
}

/* 功能菜单 - t-cell-group */
.menu-card {
  background: #ffffff;
  border-radius: 24rpx;
  margin-bottom: 24rpx;
  overflow: hidden;
  box-shadow: 
    0 8rpx 32rpx rgba(22, 93, 255, 0.08),
    0 4rpx 16rpx rgba(114, 46, 209, 0.06),
    0 2rpx 8rpx rgba(0, 0, 0, 0.04);
}

.menu-card.elderly {
  box-shadow: 
    0 40rpx 120rpx rgba(255, 138, 101, 0.12),
    0 16rpx 64rpx rgba(255, 112, 67, 0.08),
    0 8rpx 32rpx rgba(0, 0, 0, 0.06);
}

/* 退出登录单元格特殊样式 */
.logout-cell {
  --td-cell-title-color: #F5222D;
}

/* 按钮样式适配 */
.edit-btn {
  width: 100%;
  margin-top: 16rpx;
}

.manage-btn, .contact-btn {
  border-radius: 18rpx;
  padding: 10rpx 20rpx;
  font-size: 24rpx;
  font-weight: 500;
  transition: all 0.3s ease;
  display: flex;
  align-items: center;
  gap: 6rpx;
  min-width: 100rpx;
  justify-content: center;
}

.manage-btn {
  background: linear-gradient(135deg, #f8f9fa, #e9ecef);
  color: #495057;
  border: 1rpx solid #dee2e6;
  box-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.05);
}

.manage-btn:active {
  background: linear-gradient(135deg, #e9ecef, #dee2e6);
  transform: translateY(1rpx);
  box-shadow: 0 1rpx 2rpx rgba(0, 0, 0, 0.1);
}

.contact-btn {
  background: linear-gradient(135deg, #FF6B35, #E85D22);
  color: white;
  border: none;
  box-shadow: 
    0 3rpx 8rpx rgba(255, 107, 53, 0.3),
    0 1rpx 4rpx rgba(255, 107, 53, 0.2);
}

.contact-btn:active {
  background: linear-gradient(135deg, #E85D22, #D14A12);
  transform: translateY(1rpx);
  box-shadow: 
    0 2rpx 6rpx rgba(255, 107, 53, 0.4),
    0 1rpx 3rpx rgba(255, 107, 53, 0.3);
}

.binding-code-btn {
  width: 100%;
  margin-top: 20rpx;
}

/* 提示图标样式 */
.tip-icon {
  margin-right: 12rpx;
}

/* 全局TDesign按钮图标修复 */
.t-button .t-button__content {
  display: flex !important;
  flex-direction: row !important;
  align-items: center !important;
  justify-content: center !important;
  gap: 8rpx !important;
}

.t-button .t-icon {
  margin: 0 !important;
  flex-shrink: 0 !important;
}